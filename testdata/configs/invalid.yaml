lua_routing:
  enabled: true

tenants:
  - name: "invalid-tenant"
    # Missing both path_prefix and domains
    health_interval: 30
    services:
      - name: "backend1"
        url: "http://localhost:8081"
        health: "/health"