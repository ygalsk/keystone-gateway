lua_routing:
  enabled: true
  scripts_dir: "./testdata/scripts"

tenants:
  - name: "test-tenant"
    path_prefix: "/api/"
    lua_routes: "test-routes"
    health_interval: 30
    services:
      - name: "backend1"
        url: "http://localhost:8081"
        health: "/health"

admin_base_path: "/admin"