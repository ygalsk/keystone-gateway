# Multi-script test configuration

lua_routing:
  enabled: true
  scripts_dir: "./scripts/lua"

tenants:
  - name: "multi-script-test"
    path_prefix: "/api"
    lua_routes: 
      - "auth_middleware"
      - "rate_limiter" 
      - "api_routes"