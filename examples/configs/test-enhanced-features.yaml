# Test Configuration for Enhanced Chi Features
# Tests: context caching, error handlers, request body reading, route organization

compression:
  enabled: true
  level: 5
  content_types:
    - "application/json"
    - "text/html"

lua_routing:
  enabled: true
  scripts_dir: "./scripts/lua/examples"

tenants:
  # Test tenant with pure Lua routing (no backend services needed)
  - name: "enhanced-demo"
    path_prefix: "/"
    lua_routes:
      - "enhanced-test"